# Notion Master DB 運用ルール

## 1. 基本思想
- 思考・設計・進捗管理は Notion
- 実データ・分析結果は DB / コード側
- 登録・更新は **AI → JSON → Colab 実行**のみで行う
- 手動編集は最小限（例外時のみ）

---

## 2. 階層構造ルール（最重要）
-1.ホーム（※DB外・概念）
-└ 2. ジャンル ← select（例：開発 / 学習）
-└ 3. プロジェクト ← title or select（例：投資OS）
-└ 4. ゴール ← 種別 = 設計ゴール / 中間ゴール
-└ 5. タスク ← 種別 = タスク

### ルール
- **DBに登録するのは 2〜5 のみ**
- 親子関係は `親ゴール (relation)` で必ず表現する
- ゴール（4）は複数存在してよい
- タスク（5）は必ず1つの中間ゴールに紐づく

---

## 3. 種別（type）の意味

| 種別 | 意味 |
|---|---|
| 設計ゴール | プロジェクト全体の設計図・最上位ゴール |
| 中間ゴール | 設計ゴールを達成するための機能・到達点 |
| タスク | 中間ゴールを達成するための具体作業 |

---

## 4. 登録順序ルール（厳守）

1. 設計ゴールを登録
2. 中間ゴールを登録（parent = 設計ゴール）
3. タスクを登録（parent = 中間ゴール）

※ この順序を守らないと relation が解決できない

---

## 5. 日付カラムの扱い

| カラム | ルール |
|---|---|
| 登録日 | 初回作成時のみセット（以降変更しない） |
| 更新日 | 更新時に自動 or 明示的に更新 |
| 完了日 | ステータスを「完了」にした時のみセット |

---

## 6. 更新ルール

- タイトル一致で **upsert**
- 既存行がある場合：
  - 登録日は変更しない
  - 更新日のみ更新
- 完了時：
  - ステータス = 完了
  - 完了日をセット

---

## 7. 運用フロー（標準）

1. AIと対話して設計・分解
2. AIが **構造JSON** を出力
3. `%%writefile plans/xxx.json` で保存
4. Colabで `apply_plan(plan)` 実行
5. Notionでツリー・進捗を確認

---

## 8. 禁止事項
- Notion上で階層を手動で壊す
- 親ゴールなしで中間ゴール・タスクを登録
- JSONを経由しない直接登録
